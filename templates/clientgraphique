<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Client Graphique</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100">
    <div class="max-w-2xl mx-auto mt-10 p-5 bg-white rounded-lg shadow">
        <h1 class="text-2xl font-bold text-center text-gray-700">Client Graphique</h1>
        <!-- Bouton pour se connecter au serveur -->
        <div class="mt-5">
            <button id="connecter" class="px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600"> connecter au
                serveur </button>
            <!----->
        </div> <!--  chat -->
        <div id="chat-box" class="mt-5 p-3 bg-gray-200 rounded h-64 overflow-y-auto">

        </div> <!-- Zone de saisie des messages -->
        <div class="mt-5 flex">
            <input id="message-input" type="text" class="flex-1 p-2 border rounded"
                placeholder="Tapez votre message...">
            <button id="send-btn"
                class="ml-2 px-4 py-2 bg-green-500 text-white rounded hover:bg-green-600">Envoyer</button>
        </div> <!-- Bouton pour entrer dans le jeu "Le Plus Long Mot" -->
        <div class="mt-5">
            <button id="game-btn" class="px-4 py-2 bg-purple-500 text-white rounded hover:bg-purple-600"> Entrer dans le
                jeu : Le Plus Long Mot </button>
        </div>
    </div> <!-- Script Js -->
    <script>
        const connectBtn = document.getElementById("connecter");
        const sendBtn = document.getElementById("send-btn");
        const gameBtn = document.getElementById("game-btn");
        const messageInput = document.getElementById("message-input");
        const chatBox = document.getElementById("chat-box");
        // Fonction pour afficher les messages function displayMessage(message) 
        const messageElement = document.createElement("div");

        //1 afficher le message envoyer par le serveur les lettres doivent etre dans des cases décorées  
        // il faut creer aussi une autre colonne vide dans la quelle on peut manipuler et reprganiser l'ordre des lettre
        // creer un bouton pour valider le mot

        <!---------->